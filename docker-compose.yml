services:
  agentic-qe-bot:
    build: .
    container_name: qe_support_bot
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      # Persist FAISS vector store
      - ./rag/faiss_index:/app/rag/faiss_index
    restart: unless-stopped